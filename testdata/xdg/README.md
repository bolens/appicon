# XDG fixtures

Fake FreeDesktop trees for unit tests:

- `share/` — applications, icons (hicolor + Adwaita), pixmaps
- `flatpak/exports/share/` — Flatpak-style exported `.desktop` + icon
- `snap/desktop/` — Snapd-style `/var/lib/snapd/desktop` layout
